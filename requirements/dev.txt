-r base.txt
pdfplumber>=0.11
# Epingle exactement (pas ">=") : ruff a rendu stable son formatage des
# blocs de code Python dans les fichiers Markdown entre deux versions
# mineures (0.15.x -> 0.16.x), ce qui a casse silencieusement le job CI
# "lint" sur docs/AI_MODULE.md sans aucun changement de code applicatif.
# Reevaluer/mettre a jour cette epingle deliberement (jamais par derive
# d'une resolution ">=") avant d'introduire un nouveau bloc de code dans
# un fichier docs/*.md.
ruff==0.16.5
mypy>=1.13
django-stubs[compatible-mypy]>=5.1
types-python-dateutil>=2.9
pytest>=8.3
pytest-django>=4.9
pytest-asyncio>=0.24
pytest-cov>=5.0
pytest-xdist>=3.6
factory_boy>=3.3
hypothesis>=6.115
freezegun>=1.5
beautifulsoup4>=4.12
pre-commit>=4.0
django-debug-toolbar>=4.4
ipython>=8.28
pytest-playwright>=0.5
# Sprint 15 (recette UX) : audit accessibilite automatise reel (axe-core)
# sur un echantillon d'ecrans representatif, en complement de
# tests/ui/test_accessibility.py (labels/aria-label, sans regles WCAG
# completes) -- cf. docs/planning/2026-refonte-ux-sprints.md Sprint 15.
axe-playwright-python>=0.1.8
django-test-migrations>=1.4
bandit[toml]>=1.7
pip-audit>=2.7
schemathesis>=3.30
